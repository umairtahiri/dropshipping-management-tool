import { styled } from "@mui/material/styles";

export const DropshippingLayout = styled("div")(({ theme }) => ({
  background: theme.palette.background.paper,
  overflow: "hidden",
  height: "100vh",
}));
